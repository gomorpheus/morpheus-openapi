name: type
in: query
description: If specified will return all credentials by type code.
schema:
  type: string
  enum:
  - access-key-secret
  - api-key
  - client-id-secret
  - email-private-key
  - oauth2
  - tenant-username-keypair
  - username-api-key
  - username-keypair
  - username-password
  - username-password-keypair