name: type
in: query
description: Filter by Guidance Type.  See `Retrieves Guidance Types` for most up to date list of types.
schema:
  type: string
  enum:
    - Shutdown
    - Sizing
    - Host Capacity
    - Host Balancing
    - Datastore Capacity
    - Reservation Recommendation
    - Orphaned Resources