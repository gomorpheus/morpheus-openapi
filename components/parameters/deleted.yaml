name: deleted
in: query
description: If true, only deleted resources or instances in pending removal status are returned.
schema:
  type: boolean
  description: Show only deleted resources or instances in pending removal status
example: true