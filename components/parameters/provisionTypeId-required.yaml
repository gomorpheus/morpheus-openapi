name: provisionTypeId
in: query
description: Provision type filter, restricts query to only load service plans of specified provision type
required: true
schema:
  type: integer
  format: int64
example: 22