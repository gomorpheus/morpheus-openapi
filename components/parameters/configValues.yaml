name: config
in: query
description: Input parameters are required if the input is dependent on them.  Fields must be prefixed with `config.`
explode: true
style: form
schema:
  type: string
  example:
    config.fieldName1: value1