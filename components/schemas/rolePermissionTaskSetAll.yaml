type: object
required:
  - allTaskSets
  - access
properties:
  allTaskSets:
    type: boolean
    description: "Apply to all workflows (taskSets)"
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - none