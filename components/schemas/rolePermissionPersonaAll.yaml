type: object
required:
  - allPersonas
  - access
properties:
  allPersonas:
    type: boolean
    description: "Apply to all personas"
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - none