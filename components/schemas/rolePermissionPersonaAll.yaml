type: object
required:
  - allPersonas
  - access
properties:
  allTasks:
    type: boolean
    description: "Apply to all personas"
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - none