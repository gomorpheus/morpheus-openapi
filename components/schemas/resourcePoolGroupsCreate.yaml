type: object
properties: 
  name: 
    type: string
  description: 
    type: string
  mode:
    type: string
    description: Pool selection mode. Valid values are `roundrobin` or `availablecapacity`.
  pools: 
    type: array
    items: 
      type: integer
      format: int64