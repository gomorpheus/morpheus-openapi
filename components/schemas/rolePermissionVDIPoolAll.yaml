type: object
required:
  - allVdiPools
  - access
properties:
  allTasks:
    type: boolean
    description: "Apply to all VDI Pools"
  access:
    type: string
    description: The new access level.
    enum:
      $ref: rolePermissionAccessLevelsForResource.yaml