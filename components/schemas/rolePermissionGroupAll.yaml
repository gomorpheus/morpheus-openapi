type: object
required:
  - allGroups
  - access
properties:
  groupId:
    type: boolean
    description: "Apply to all group (site)"
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - read
      - none