type: object
required:
  - permissionCode
  - access
properties:
  permissionCode:
    type: string
    description: "`AppTemplate` is the code for Global Blueprint Access"
    enum:
      - AppTemplate
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - custom
      - none