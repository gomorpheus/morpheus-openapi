title: ApiForgotSendEmailRequest
required:
  - username
type: object
properties:
  username:
    type: string
    description: >-
      Specified as "username" or "tenantId\username" with proper HTML encoding
      and used in conjunction with `password`.
