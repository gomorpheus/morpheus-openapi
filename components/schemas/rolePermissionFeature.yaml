title: Feature Permission
type: object
required:
  - permissionCode
  - access
properties:
  permissionCode:
    type: string
    description: The code of the feature permission being changed
    enum:
      $ref: rolePermissionCodes.yaml
  access:
    type: string
    description: The new access level.
    enum:
      $ref: rolePermissionAccessLevels.yaml