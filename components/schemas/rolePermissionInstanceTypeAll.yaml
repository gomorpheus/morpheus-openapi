type: object
required:
  - allAppTemplates
  - access
properties:
  allAppTemplates:
    type: boolean
    description: "Apply to all instance types"
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - none