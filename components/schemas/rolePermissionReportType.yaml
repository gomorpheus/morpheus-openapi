type: object
required:
  - reportTypeId
  - access
properties:
  reportTypeId:
    type: integer
    description: "`id` of the report type"
  access:
    type: string
    description: The new access level.
    enum:
      $ref: rolePermissionAccessLevelsForResource.yaml