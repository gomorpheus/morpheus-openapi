type: object
required:
  - permissionCode
  - access
properties:
  permissionCode:
    type: string
    description: "`TaskSet` is the code for Default Workflow Access"
    enum:
      - Tasks
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - none