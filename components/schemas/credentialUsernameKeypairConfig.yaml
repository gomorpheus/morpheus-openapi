required:
  - username
  - authKey
properties:
  username:
    type: string
    description: Username
    example: jsmith
  authKey:
    type: object
    required:
      - id
    properties:
      id:
        type: integer
        format: int64
        description: Private Key ID of a `Key Pair`
        example: 9