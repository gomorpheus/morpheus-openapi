type: object
required:
  - permissionCode
  - access
properties:
  permissionCode:
    type: string
    description: "`ComputeSite` is the code for Global Group Access"
    enum: 
      - ComputeSite
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - custom
      - read
      - none