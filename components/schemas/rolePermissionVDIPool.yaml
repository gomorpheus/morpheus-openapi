type: object
required:
  - vdiPoolId
  - access
properties:
  vdiPoolId:
    type: integer
    description: "`id` of the VDI Pool"
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - none