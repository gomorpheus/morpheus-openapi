type: object
required:
  - allAppTemplates
  - access
properties:
  allAppTemplates:
    type: boolean
    description: "Apply to all blueprints (appTemplate)"
  access:
    type: string
    description: The new access level.
    enum:
      $ref: rolePermissionAccessLevelsForResource.yaml