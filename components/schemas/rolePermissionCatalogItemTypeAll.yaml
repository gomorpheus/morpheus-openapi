type: object
required:
  - allCatalogItemTypes
  - access
properties:
  allCatalogItemTypes:
    type: boolean
    description: "Apply to all catalog item types"
  access:
    type: string
    description: The new access level.
    enum:
      $ref: rolePermissionAccessLevelsForResource.yaml