type: object
required:
  - groupId
  - access
properties:
  groupId:
    type: integer
    description: "`id` of the group (site)"
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - read
      - none