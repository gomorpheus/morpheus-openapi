type: object
required:
  - instanceTypeId
  - access
properties:
  instanceTypeId:
    type: integer
    description: "`id` of the instance type"
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - none