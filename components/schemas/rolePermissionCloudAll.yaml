type: object
required:
  - allClouds
  - access
properties:
  allClouds:
    type: boolean
    description: "Apply to all clouds"
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - read
      - none