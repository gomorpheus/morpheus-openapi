title: HubRegisterObject
allOf:
  - $ref: .\BaseType.yaml
  - required:
      - hub
    type: object
    properties:
      hub:
        $ref: .\Hub1.yaml
description: >-
  Object for registering with the [Morpheus Hub](https://morpheushub.com). This
  is only required for `hubmode=register`.
