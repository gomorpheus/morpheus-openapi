type: object
description: The standard response object that is returned when an operation is successful
properties:
  success:
    type: boolean
    description: >-
      Success indicator, true when the request succeeded and false when an error
      occurred
    default: true
example:
  success: true
