title: Default Report Type Permission
type: object
required:
  - permissionCode
  - access
properties:
  permissionCode:
    type: string
    description: "`ReportTypes` is the code for Default Report Type Access"
    enum:
      - ReportTypes
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - none