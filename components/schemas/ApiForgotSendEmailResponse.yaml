title: ApiForgotSendEmailResponse
type: object
properties:
  success:
    type: boolean
    examples:
      - true
  msg:
    type: string
    examples:
      - >-
        Reset password instructions have been sent to the user 'jsmith', if they
        exist.
