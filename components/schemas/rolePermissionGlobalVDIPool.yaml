type: object
required:
  - permissionCode
  - access
properties:
  permissionCode:
    type: string
    description: "`VdiPools` is the code for Global VDI Pool Access"
    enum:
      - VdiPools
  access:
    type: string
    description: The new access level.
    enum:
      - full
      - custom
      - none