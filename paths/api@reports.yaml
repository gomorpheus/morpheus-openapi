get:
  summary: Returns all reports
  operationId: getAllReports
  description: |
    This endpoint returns all reports. This is results of reports that have been executed in the past.
  externalDocs:
    url: https://apidocs.morpheusdata.com/#get-all-reports
  parameters:
    - $ref: ../components/parameters/max.yaml
    - $ref: ../components/parameters/offset.yaml
    - $ref: ../components/parameters/sort.yaml
    - $ref: ../components/parameters/direction.yaml
    - $ref: ../components/parameters/phrase.yaml
    - $ref: ../components/parameters/name.yaml
    - name: reportType
      in: query
      description: If specified will return an exact match on report type code, accepts multiple values
      schema:
        type: string
    - $ref: ../components/parameters/category.yaml
  responses:
    '200':
      description: Successful Request
      content:
        application/json:
          schema:
            $ref: ../components/schemas/200-reports.yaml
    '400':
      description: Bad Request.
    '401':
      description: Unauthorized.
    '403':
      description: Forbidden.
    '404':
      description: Object Not Found.
    '500':
      description: Internal Server Error.
    '503':
      description: Service Unavailable.
  tags:
    - Reports
post:
  summary: Run specified report
  operationId: runReport
  description: |
    This endpoint execute the specified report type and create a new report result.

    The available parameters vary by report type. Refer to the defined `inputs` for each report.
  consumes:
    - application/json
  parameters:
    - in: body
      name: Body
      required: true
      description: Wiki Page to Update.  Only the values you wish to update should be in the body payload.
      schema:
        type: object
        required:
          - type
        properties:
          report:
            type: object
            properties: 
              type: 
                type: string
                example: appCost
              startDate: 
                type: string
                format: date
                example: 2019-01-01
              endDate: 
                type: string
                format: date
                example: 2020-01-01
              groupId:
                description: The Group ID filter for the report
                type: number
                example: 1
              cloudId:
                description: The Cloud ID filter for the report
                type: number
                example: 7
  externalDocs:
    url: https://apidocs.morpheusdata.com/#run-a-report
  responses:
    '200':
      description: Successful Request
      content:
        application/json:
          schema:
            $ref: ../components/schemas/200-user.yaml
    '400':
      description: Bad Request.
    '401':
      description: Unauthorized.
    '403':
      description: Forbidden.
    '404':
      description: Object Not Found.
    '500':
      description: Internal Server Error.
    '503':
      description: Service Unavailable.
  tags:
    - Reports